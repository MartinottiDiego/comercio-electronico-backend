import type { StrapiApp } from '@strapi/strapi/admin';

export default {
  bootstrap(app) {
    // Escribir tu lógica de bootstrap aquí
  },
  register(app) {
    // Escribir tu lógica de registro aquí
  },
  destroy(app) {
    // Escribir tu lógica de destrucción aquí
  },
};
